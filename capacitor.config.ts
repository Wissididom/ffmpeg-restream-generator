import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'generator.restream.ffmpeg',
  appName: 'ffmpeg-restream-generator',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
